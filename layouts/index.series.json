{{- /* Generate data used to populate a select in Pages CMS */}}
{{- $s := newScratch }}
{{- $s.Add "series" slice }}
{{- range $series, $pages := site.Taxonomies.series }}
  {{- $s.Add "series" (dict "id" $series "name" $series) }}
{{- end }}
{{- $s.Get "series" | jsonify }}
